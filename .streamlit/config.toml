[theme]
base = "dark"
primaryColor = "#E0E0E0"
backgroundColor = "#02040a"
secondaryBackgroundColor = "#0a0c10"
textColor = "#E0E0E0"
font = "serif"
[theme]
primaryColor = "#007AFF"    # iOS 经典的系统蓝 (用于按钮、滑块)
backgroundColor = "#F2F2F7" # iOS 系统背景灰 (不完全是纯白，看着更舒服)
secondaryBackgroundColor = "#FFFFFF" # 卡片和侧边栏的纯白背景
textColor = "#1C1C1E"       # iOS 深灰色字体 (比纯黑更柔和)
font = "sans serif"         # 无衬线字体